-- =============================================================================
-- 0017_company_documents_raw_text.sql
-- company_documents にテキスト抽出カラムを追加
-- =============================================================================
-- 実行日: 2026-01-22
-- 説明:
--   - raw_text: PDFから抽出したテキスト（PDF.jsでクライアント側抽出）
--   - これにより、抽出ボタン押下時に再度PDFを選択する必要がなくなる
-- =============================================================================

-- raw_text カラムを追加（既に存在する場合はスキップ）
ALTER TABLE company_documents ADD COLUMN raw_text TEXT;
