{
  "name": "subsidy-job-submit",
  "version": "1.0.0",
  "description": "Job Submit Lambda for Subsidy Matching System",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:@aws-sdk/*",
    "package": "npm run build && cd dist && zip -r function.zip index.js",
    "deploy": "npm run package && aws lambda update-function-code --function-name subsidy-app-dev-job-submit --zip-file fileb://dist/function.zip"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.500.0",
    "@aws-sdk/client-sqs": "^3.500.0",
    "jose": "^5.2.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/node": "^20.11.0",
    "@types/uuid": "^9.0.7",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
